<% if view_signed_in? %>
<% if current_view.role == "Customer"%>

<% else %>
  <% @customers.each do |cu| %>
    <div class="card-body">
      <div class="card-header ">
        <%= link_to cu.full_name, user_detail_path(cu.id, @workshop), method: :get  %><br>
        <%= cu.email %>
      </div>
    </div>
  <% end %>
<% end %>
<% end %>

<div class="container">
  <div class= "text-center"  style="width: 72rem;">
    <div class="card-body " id="flip-card-inner ">
      <div class="card-header ">
       <%= image_tag 'dummy-user.png', class: 'mr-3 guest-icon' %><br>
        <ul class="mt-4">
      <div >Your name is <strong><%= current_view.full_name %>.</strong></div>
      <div >You contact number is <strong><%= current_view.contact_number %> .</strong></div>
      <div>You role is <strong><%= current_view.role %>.</strong></div>
    </ul>
<% @customers.each do |c| %>
  
<% if view_signed_in? %>
<% if current_view.email == c.email%>
<div class="container ">
	<% if current_view.role == "Admin" %>
		<div class ="container my-4 ">
		<div class="card text-center"  style="width: 42rem;">
  <div class="card-body">
  <div class="card-header ">
  
   <%= link_to c.full_name, user_detail_path(c.id, @workshop), method: :get  %><br>
  <%= c.email %>
	</div>
	</div>
	</div>
<% end %>
	</div>
	</div>
	</div>
	</div>
<% end %>
<% end %>
<% end %>


<% @customers.each do |c| %>
<% if view_signed_in? %>
<% if current_view.email == c.email%>
  <div class="card my-4 text-center"  style="width: 72rem;">
    <div class="card-body">
      <div class="card-header ">
        <p style= "color:blue;">Total Number of course u booked :-   <%= c.bookings.count %><br> </p>
        <table  class="table table-striped table-dark table-bordered ">
        <tr>
          <th class="text-center" id="body">Course name</th>
          <th class="text-center" id="body">Price </th>
          <th class="text-center" id="body"> Date-of-event</th>
          <th class="text-center" id="body"> Time-of-event</th>
          <th class="text-center" id="body"> Tickets </th>
          <th class="text-center" id="body">Total amount </th>
          <th class="text-center" id="body">Booking Id </th>
        </tr>
        </table>
        <% c.bookings.each do |b| %>
        <table class="table table-striped">
        <tr>
          <td  class="text-center" ><%= b.workshop.name %><br></td>
          <td  class="text-center"><%= b.workshop.registration_fee %><br></td>
          <td  class="text-center"><%= b.workshop.start_date %> to <%= b.workshop.end_date %><br></td>
          <td  class="text-center"> Daily <%= b.workshop.start_time %> to <%= b.workshop.end_time %><br></td>
          <td  class="text-center"><%= b.no_of_tickets %><br></td>
          <td  class="text-center"><%= b.amount_paid %><br></td>
          <td  class="text-center"><%= b.order_number %><br></td>
          </tr>
        </table>
        <% end %>
       
       <hr>
      <p class= "text-center"><b>Want to cancel your booking ⁉️ </b>
      <%= link_to "Cancel your booking" , new_refund_path, target: "_blank" %></p>
			</div>
		</div>
	</div>

<% end %>
<% end %>
<% end %>


<style>
 #body {
  font-family: Arial, Helvetica, sans-serif;
}
 tbody tr:nth-child(odd) {
  background-color: lightyellow;
}

th {
  letter-spacing: 2px;
}

td {
  letter-spacing: 1px;
}

</style>